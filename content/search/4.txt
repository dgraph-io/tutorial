{
  r(func: regexp(name, //)) {
    name

    ~genre @filter(regexp(name, //)){
      name
      starring {
        performance.actor @filter(regexp(name, //)) {
          name
        }
      }
    }
  }
}
